#!/usr/bin/env julia
import Base.Threads

# TODO: there isn't a single-threaded Julia implementation of this one, so it would require more work.
# Thus, it might be better to start with the other two and advance to this one.
# That, or use Rodinia (and other implementations) to guide the work.